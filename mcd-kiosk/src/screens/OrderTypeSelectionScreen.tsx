export default function OrderTypeSelectionScreen() {
    // const setOrderType = useKioskStore((state) => state.setOrderType);
    return (
      <div>
        <h1>What would you like to do ? </h1>
        <h1>dine in</h1>
        <h1>take away</h1>
        {/* <button onClick={() => setOrderType('dine-in')}>Dine In</button> */}
        {/* <button onClick={() => setOrderType('takeaway')}>Take Away</button> */}
      </div>
    );
  }